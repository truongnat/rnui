[install]
# Bun workspace install strategy
hoist = true
linker = "isolated"

[install.lockfile]
# Save lockfile on install
save = true
print = "bun"

[run]
# Always use Bun's JS runtime for scripts
bun = true
