---
wave: 1
depends_on: []
phase: 09-improvement-plan-phase3
plan: 01
type: execute
requirements:
  - IMP-09
  - IMP-10
  - IMP-11
  - IMP-12
requirements_addressed:
  - IMP-09
  - IMP-10
  - IMP-11
  - IMP-12
files_modified:
  - packages/ui/src/components/Skeleton/Skeleton.tsx
  - packages/ui/src/components/Skeleton/index.ts
  - packages/ui/src/components/EmptyState/EmptyState.tsx
  - packages/ui/src/components/EmptyState/index.ts
  - packages/headless/src/tokens/component.ts
  - packages/ui/src/components/Select/Select.tsx
  - packages/headless/src/hooks/useSlider.ts
  - packages/headless/src/index.ts
  - packages/ui/src/components/Slider/Slider.tsx
  - packages/ui/src/index.ts
  - apps/example/app/index.tsx
autonomous: true
gap_closure: false
must_haves:
  truths:
    - "Skeleton: có preset mới (SkeletonProfile/Media/Form/Grid/Table hoặc tập con đã chốt) + SkeletonGroup stagger"
    - "EmptyState: size / variant / illustration; preset default copy cho search|error|offline|permission|empty"
    - "Select: danh sách option trong sheet dùng FlashList; có loading + onLoadMore + hasMore + loadingMore; example large list + load-more"
    - "Slider: orientation horizontal|vertical; range dual-thumb; thumbRenderer; example vertical + range + custom thumb"
    - "IMP-13 (DatePicker calendar grid) không nằm trong plan này — defer 09-02"
  artifacts:
    - path: packages/ui/src/components/Skeleton/Skeleton.tsx
      provides: Presets + SkeletonGroup + optional shimmer direction
    - path: packages/ui/src/components/EmptyState/EmptyState.tsx
      provides: Props API mở rộng + layout/visual theo IMPROVEMENT_PLAN Issue 9
    - path: packages/ui/src/components/Select/Select.tsx
      provides: FlashList + async/load-more props
    - path: packages/headless/src/hooks/useSlider.ts
      provides: Orientation + range tuple API cho Slider UI
    - path: packages/ui/src/components/Slider/Slider.tsx
      provides: UI vertical, range, custom thumb
  key_links:
    - from: packages/headless/src/hooks/useSlider.ts
      to: packages/ui/src/components/Slider/Slider.tsx
      via: hook return (gesture, layout, value state) phải khớp props mới
    - from: packages/ui/src/components/Select/Select.tsx
      to: apps/example/app/index.tsx
      via: demo LARGE_COUNTRIES + load-more state machine
---

# Plan 09-01: Phase 9 — IMPROVEMENT_PLAN Phase 3 (tính năng mới)

**Nguồn:** `IMPROVEMENT_PLAN.md` — *Execution Order → Phase 3 — New Features* (mục 9–12) + Issue Details (Issues 4, 6, 8, 9, 13).

**REQ-IDs (ánh xạ 1:1 với Execution Order Phase 3):**

| REQ-ID | Execution Order | IMPROVEMENT_PLAN Issue # | Nội dung ngắn |
|--------|-----------------|--------------------------|---------------|
| **IMP-09** | 9 | Issue 8 (Skeleton) | Preset Skeleton + `SkeletonGroup` + shimmer direction |
| **IMP-10** | 10 | Issue 9 (EmptyState) | Nâng visual + `size` / `variant` / `illustration` |
| **IMP-11** | 11 | Issue 4 (Select) | FlashList + `loading` / `onLoadMore` / `hasMore` / `loadingMore` |
| **IMP-12** | 12 | Issue 6 (Slider) | `useSlider` orientation + range; UI vertical / thumb / range |
| **IMP-13** | 13 | Issue 13 (DatePicker) | **Không thực hiện trong 09-01** — tách `09-02-PLAN.md` (effort XL) |

## Đã hoàn thành (không lặp lại trong plan này)

Theo `IMPROVEMENT_PLAN.md` **Phase 1–2** và roadmap Phase **7–8**:

- **Phase 1 (Execution Order 1–4):** OTPInput, Toast, AnimatedList example (`useCallback`), SegmentedControl — **IMP-01..IMP-04**
- **Phase 2 (Execution Order 5–8):** Input/FormField, TextArea counter, Typography + tokens, Divider/Radio/Checkbox showcases + `RadioItem` — **IMP-05..IMP-08**

Không thêm task trùng các mục trên.

## Thứ tự phụ thuộc (dependency order)

```text
[IMP-09 Skeleton] ─┐
                   ├─► song song (không đụng file chung bắt buộc)
[IMP-10 EmptyState]┘

[IMP-11 Select] — độc lập với 9/10 về logic; **sau khi** có FlashList pattern ổn định trong Select, mới **wire example** load-more (cùng task hoặc sub-step cuối).

[IMP-12] — **bắt buộc:** `packages/headless/.../useSlider.ts` (contract + gesture) **trước** `Slider.tsx` UI + example.
```

---

## Task 1 — IMP-09: Skeleton presets + SkeletonGroup

<action>

1. `Skeleton.tsx` (và file preset hiện có): thêm các preset theo `IMPROVEMENT_PLAN.md` Issue 8 — `SkeletonProfile`, `SkeletonMedia`, `SkeletonForm`, `SkeletonGrid`, `SkeletonTable` (hoặc subset tối thiểu có lý do trong SUMMARY nếu cần cắt scope).
2. Thêm `SkeletonGroup` với prop `stagger` (ms) — delay animation theo thứ tự child (hoặc pattern tương đương document trong code).
3. Thêm `shimmerDirection` (hoặc tên đã thống nhất): `"left-to-right"` | `"right-to-left"` | `"pulse"` — map sang logic shimmer hiện tại.
4. Export công khai qua `packages/ui/src/components/Skeleton/index.ts` và barrel `packages/ui/src/index.ts` nếu cần.
5. `apps/example/app/index.tsx`: section Skeleton — demo vài preset + `SkeletonGroup` (có thể dùng `SegmentedControl`/tab đã có để chuyển preset).

**Files dự kiến:** `packages/ui/src/components/Skeleton/**`, `packages/ui/src/index.ts`, `apps/example/app/index.tsx`; có thể `packages/headless/src/tokens/component.ts` nếu thêm token skeleton group.

</action>

<acceptance_criteria>

- Example hiển thị rõ ít nhất 2 preset mới + một hàng `SkeletonGroup` với stagger nhìn thấy được.
- `bun turbo typecheck --filter=@truongdq01/ui` pass (hoặc lệnh typecheck monorepo tương đương trong repo).

</acceptance_criteria>

<verify>

```bash
bun turbo typecheck --filter=@truongdq01/ui
bun turbo test --filter=@truongdq01/ui
```

</verify>

---

## Task 2 — IMP-10: EmptyState — layout, variant, illustration

<action>

1. Mở rộng `EmptyStateProps`: `size?: "sm" | "md" | "lg"`, `illustration?: ReactNode`, `variant?: "default" | "search" | "error" | "offline" | "permission" | "empty"` (điều chỉnh tên nếu trùng reserved — giữ semantic).
2. Khi `variant` được set và không override title/description/icon — dùng copy + icon mặc định theo bảng Issue 9 (`SearchX`, `AlertCircle`, `WifiOff`, `ShieldOff`, `Inbox`, … — dùng `Icon` sẵn có).
3. Visual: container icon ~64×64, nền `brand.subtle` (hoặc token emptyState), typography title/description theo plan; padding dọc đủ (`spacing[8]` hoặc token).
4. Cập nhật token `emptyState` trong headless `component.ts` nếu cần field mới (spacing theo size).
5. Example: showcase 5 preset `variant` (carousel/stack — khớp pattern app).

**Files dự kiến:** `packages/ui/src/components/EmptyState/EmptyState.tsx`, `packages/headless/src/tokens/component.ts` (emptyState), `apps/example/app/index.tsx`.

</action>

<acceptance_criteria>

- Mỗi `variant` có thể render chỉ bằng prop (không bắt buộc truyền title tay).
- Không regression typecheck; a11y: `Text` vẫn đọc được (hoặc thêm `accessibilityRole` nếu đã dùng pattern repo).

</acceptance_criteria>

<verify>

```bash
bun turbo typecheck --filter=@truongdq01/ui
bun turbo test --filter=@truongdq01/ui
```

</verify>

---

## Task 3 — IMP-11: Select — FlashList + loading + load-more

<action>

1. Thêm props vào `Select` (API cụ thể khớp Issue 4): `loading?`, `onLoadMore?`, `hasMore?`, `loadingMore?`, `renderOption?` (generic an toàn kiểu `T`).
2. Thay phần `ScrollView` + `filtered.map` trong bottom sheet content bằng **FlashList** (`@shopify/flash-list`) — `estimatedItemSize={48}` (hoặc đo thực tế), `keyExtractor` ổn định.
3. `ListFooterComponent`: spinner khi `loadingMore`; `onEndReached` gọi `onLoadMore` khi `hasMore` (guard chống gọi lặp vô hạn).
4. `ListEmptyComponent`: khi `loading===true` — skeleton list (dùng `SkeletonListItem` ×3 từ IMP-09).
5. (Tùy chọn trong cùng PR) Section header: `options` dạng section — **chỉ nếu** không phình scope; nếu không, ghi rõ defer trong SUMMARY.
6. **Example sau cùng:** mảng `LARGE_COUNTRIES` (~50), state `page` hoặc mock delay — chứng minh `onLoadMore` + `hasMore`.

**Files dự kiến:** `packages/ui/src/components/Select/Select.tsx`, có thể `packages/ui/package.json` nếu FlashList chưa là dependency trực tiếp của `ui` (hiện có qua AnimatedList — kiểm tra import path).

**Phụ thuộc:** Hoàn thành Task 1 đủ để dùng skeleton empty loading (hoặc tạm dùng placeholder `Text` "Loading…" rồi thay skeleton trong cùng wave nếu merge order khác).

</action>

<acceptance_criteria>

- Scroll mượt với list lớn trong example; end-reach kích hoạt load-more (log hoặc UI footer).
- Typecheck pass cho package `ui`.

</acceptance_criteria>

<verify>

```bash
bun turbo typecheck --filter=@truongdq01/ui
bun turbo test --filter=@truongdq01/ui
```

</verify>

**Kiểm thử thủ công (example):**

- Mở Select lớn, kéo tới cuối → thấy load-more (mock).
- Search vẫn lọc `filtered` trước khi đưa vào FlashList.

---

## Task 4 — IMP-12a: Headless — `useSlider` orientation + range

<action>

1. Mở rộng `UseSliderOptions` / `UseSliderReturn` trong `useSlider.ts`:
   - `orientation?: "horizontal" | "vertical"` (default `"horizontal"`).
   - Range: `value` có thể là `[number, number]` khi `range: true`; `onChange` / `onChangeEnd` phát tuple tương ứng; hai thumb ratio hoặc API rõ ràng (document trong JSDoc).
2. Vertical: đo **chiều cao** track thay vì width; pan gesture dùng `translationY`; phần trăm map theo trục dọc.
3. Xuất hook từ `packages/headless/src/index.ts` nếu chưa re-export; thêm/ cập nhật test hook nếu repo có pattern test Reanimated (hoặc unit thuần cho snap math).

**Files dự kiến:** `packages/headless/src/hooks/useSlider.ts`, `packages/headless/src/index.ts`, `packages/headless/src/hooks/__tests__/*` (nếu có).

</action>

<acceptance_criteria>

- `bun turbo typecheck --filter=@truongdq01/headless` pass.
- Test (nếu thêm): snap + min/max không cross cho range (ít nhất một case).

</acceptance_criteria>

<verify>

```bash
bun turbo typecheck --filter=@truongdq01/headless
bun turbo test --filter=@truongdq01/headless
```

</verify>

---

## Task 5 — IMP-12b: UI `Slider` + example (vertical, range, custom thumb)

<action>

1. `Slider.tsx`: props `orientation`, `thumbRenderer`, `range`, `rangeValue`, `onRangeChange`, `sliderHeight` (vertical) — khớp Issue 6; dùng `useSlider` đã mở rộng.
2. Range: hai thumb, fill giữa hai đầu, không cho thumb vượt qua nhau (clamp).
3. Custom thumb: nếu `thumbRenderer` thì render node đó trong khung vị trí thumb (giữ `Animated.View` wrapper để layout).
4. Example: ba demo — vertical (`orientation="vertical"`, height ~150), custom icon (`Star` hoặc icon có sẵn), range `[20, 80]`.

**Files dự kiến:** `packages/ui/src/components/Slider/Slider.tsx`, `packages/ui/src/index.ts`, `apps/example/app/index.tsx`.

</action>

<acceptance_criteria>

- Ba demo chạy được trên example app; không lỗi đỏ Metro liên quan Slider.
- Typecheck `ui` pass.

</acceptance_criteria>

<verify>

```bash
bun turbo typecheck --filter=@truongdq01/ui
bun turbo test --filter=@truongdq01/ui
```

</verify>

**Kiểm thử thủ công:**

- Kéo thumb single/range; vertical: kéo lên/xuống đúng hướng.

---

## Tách scope — Phase 4–5 (follow-on, không nằm trong 09-01)

| IMPROVEMENT_PLAN | Nội dung | Gợi ý phase |
|------------------|----------|-------------|
| Phase 4 (14–17) | GlassCard, gradient + LinearGradient, Carousel/contrast, color refinement | Phase 10+ |
| Phase 5 (18–19) | COMPONENT_TREE.md, cập nhật UI_REPORT.md | Phase 10+ doc wave |

---

## IMP-13 — Defer tới `09-02-PLAN.md`

**Lý do tách:** Effort **XL (~16h+)**, phụ thuộc `CalendarGrid.tsx`, `CalendarBottomSheet.tsx`, refactor `DatePicker.tsx` iOS — vượt ngữ cảnh một executor wave đơn.

**Phạm vi tương lai (ghi nhận):** theo Issue 13 — BottomSheet + lưới lịch, prop `pickerStyle`, giữ Android native; tùy chọn Lunar defer.

---

## Verification tổng

- Lệnh tự động:

```bash
bun turbo typecheck
bun turbo test --filter=@truongdq01/ui --filter=@truongdq01/headless
```

- Checklist thủ công: `09-VALIDATION.md` (tạo khi execute-phase nếu repo dùng pattern 08-VALIDATION).

---

## Security verification

- **pass** — chỉ UI/components/example; không secrets; không dependency mạng mới bắt buộc (FlashList đã dùng trong monorepo).
- **Risk level:** low
- **Risks found:** none material
- **Mitigation:** N/A
- **Residual risk:** gesture edge cases trên thiết bị thật — UAT ngắn trên iOS/Android

---

## Output sau khi chạy xong

Tạo `.planning/phases/09-improvement-plan-phase3/09-01-SUMMARY.md` theo template `summary.md` (nếu execute-phase yêu cầu).
