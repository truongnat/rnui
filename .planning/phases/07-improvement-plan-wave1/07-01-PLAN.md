---
wave: 1
depends_on: []
phase: 07-improvement-plan-wave1
plan: 01
type: execute
requirements:
  - IMP-01
  - IMP-02
  - IMP-03
  - IMP-04
requirements_addressed:
  - IMP-01
  - IMP-02
  - IMP-03
  - IMP-04
files_modified:
  - apps/example/app/index.tsx
  - packages/tokens/src/component.ts
  - packages/ui/src/components/OTPInput/OTPInput.tsx
  - packages/ui/src/components/Toast/Toast.tsx
  - packages/ui/src/components/Toast/ToastContainer.tsx
  - packages/ui/src/components/SegmentedControl/SegmentedControl.tsx
autonomous: true
gap_closure: false
must_haves:
  truths:
    - 'OTP example is controlled; filled cells use distinct border from empty'
    - 'Toast enter/exit feels smooth without harsh spring bounce'
    - 'AnimatedList demo uses stable renderItem + keyExtractor'
    - 'SegmentedControl indicator aligns after layout; no width=0 flash'
  artifacts:
    - path: apps/example/app/index.tsx
      provides: OTP + AnimatedList + Carousel data fixes as scoped in tasks
---

# Plan 07-01: Phase 7 — Wave 1 (critical UX from IMPROVEMENT_PLAN)

**Nguồn:** `IMPROVEMENT_PLAN.md` _Execution Order → Phase 1_ (OTP, Toast, AnimatedList, SegmentedControl).

**Goal:** Ship bốn hạng mục **IMP-01..04**; defer Input/Typography/Select/Slider/DatePicker/doc tree sang wave sau.

---

## Task 1 — IMP-01: OTPInput border + example controlled

<action>

1. `apps/example/app/index.tsx`: state `otpValue` + `setOtpValue`; `<OTPInput value={otpValue} onChange={setOtpValue} />` (bỏ `value="123"` cố định).
2. `packages/tokens` / `otpInput` tokens: thêm `filledBorderColor` (và optional `filledBg`) theo plan.
3. `packages/ui/.../OTPInput.tsx`: nhánh `borderColor` focused / filled / default đúng token; `aspectRatio: 1` (hoặc kích thước vuông từ token); border width focused vs default nếu spec có.

</action>

<acceptance_criteria>

- Gõ/xóa OTP trong example hoạt động; ô filled có border khác empty.
- `bun turbo test --filter=@truongdq01/ui` (hoặc test OTP nếu có) pass.

</acceptance_criteria>

---

## Task 2 — IMP-02: Toast animation — bớt “jump”

<action>

1. Tìm preset Reanimated trong Toast / ToastContainer (enter/exit/layout).
2. Giảm stiffness hoặc thay bằng `withTiming` / preset mềm hơn; tránh spring quá gắt trên stack nhiều toast.
3. Ghi 1 dòng comment **tại chỗ** giải thích tham số.

</action>

<acceptance_criteria>

- Không regression hiển thị toast; animation mượt hơn subjectively + không đổi API công khai trừ khi cần (tránh breaking).

</acceptance_criteria>

---

## Task 3 — IMP-03: AnimatedList example — stable callbacks

<action>

1. `apps/example/app/index.tsx`: tách `renderItem` ra `useCallback` (deps đúng); thêm `keyExtractor` ổn định (`id`).
2. Tuỳ chọn: một dòng JSDoc trong example hoặc nhắc trong `AnimatedList` docblock (đã có phần estimatedItemSize) về stable `renderItem`.

</action>

<acceptance_criteria>

- Example build chạy; không tạo function mới mỗi render cho `renderItem`.

</acceptance_criteria>

---

## Task 4 — IMP-04: SegmentedControl layout + indicator

<action>

1. `SegmentedControl.tsx`: inset indicator (padding container), chỉ render indicator sau `onLayout` khi đã đo width (`ready`).
2. Điều chỉnh `translateX` / width segment khớp padding token.
3. Kiểm tra storybook/example nếu có demo.

</action>

<acceptance_criteria>

- Không nhảy indicator lần đầu; segment chọn khớp vùng bấm.

</acceptance_criteria>

---

## Verification

- `07-VALIDATION.md`
- `bun turbo typecheck` / `bun turbo test` cho package đụng tới

---

## PLANNING NOTE

**Plans:** 1 (`07-01-PLAN.md`)  
**Waves:** 1 (wave 2 = IMPROVEMENT_PLAN Phase 2 trong file gốc)
