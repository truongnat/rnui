---
wave: 1
depends_on: []
requirements:
  - BL-01
  - BL-02
requirements_addressed:
  - BL-01
  - BL-02
files_modified:
  - packages/headless/src/hooks/useOTPInput.ts
  - packages/headless/src/hooks/__tests__/useOTPInput.test.tsx
  - packages/ui/src/components/OTPInput/OTPInput.tsx
  - packages/ui/src/components/OTPInput/__tests__/OTPInput.test.tsx
  - packages/ui/src/components/DatePicker/DatePicker.tsx
  - packages/ui/src/components/DatePicker/__tests__/DatePicker.test.tsx
autonomous: true
gap_closure: false
---

# Plan 05-01: Phase 5 — Wave 1 (OTP paste proof, DatePicker locale/TZ)

**Mode:** standard (`05-RESEARCH.md`). **UI hint:** mixed — không bắt buộc UI-SPEC trước execute; có thể `/gsd-ui-phase 5` nếu cần contract thêm.

**Goal:** Đóng **AUDIT** 5.7 (OTP paste) bằng test + hành vi rõ; **5.8** (DatePicker) bằng prop forward + test.

---

## Task 1 — `useOTPInput`: paste / autofill một lần

<action>

1. Trong `packages/headless/src/hooks/useOTPInput.ts`, xác nhận logic `handleChange`:
   - strip non-digit;
   - `slice(0, length)`;
   - gọi `onComplete` khi đủ độ dài.
2. Nếu cần (ví dụ paste có ký tự xen kẽ): giữ một đường xử lý duy nhất — không nhân đôi state.
3. Trong `packages/headless/src/hooks/__tests__/useOTPInput.test.tsx`, thêm case:
   - `length: 6`, `handleChange("123456")` → `onChange("123456")`, `onComplete` gọi;
   - `length: 4`, `handleChange("123456")` → `onChange("1234")`;
   - chuỗi có ký tự lạ `"12x34"` → `"1234"` (đã có một phần — bổ sung nếu thiếu).

</action>

<read_first>

- `packages/headless/src/hooks/useOTPInput.ts`
- `packages/headless/src/hooks/__tests__/useOTPInput.test.tsx`

</read_first>

<acceptance_criteria>

- `cd packages/headless && bun test src/hooks/__tests__/useOTPInput.test.tsx` exit 0.
- `cd packages/headless && bun tsc --noEmit` exit 0.

</acceptance_criteria>

---

## Task 2 — `OTPInput` UI: test paste qua `TextInput`

<action>

1. Thêm `testID` ổn định cho `TextInput` ẩn (vd. `testID="rnui-otp-input"`) — chỉ phục vụ test / automation.
2. Trong `OTPInput.test.tsx`, dùng `getByTestId` + `fireEvent.changeText` với chuỗi đầy đủ (6 ký tự) → assert `onChange` nhận đúng.
3. `bun tsc --noEmit` trong `packages/ui`.

</action>

<read_first>

- `packages/ui/src/components/OTPInput/OTPInput.tsx`
- `packages/ui/src/components/OTPInput/__tests__/OTPInput.test.tsx`

</read_first>

<acceptance_criteria>

- `rg "rnui-otp-input" packages/ui/src/components/OTPInput/OTPInput.tsx` có match.
- `cd packages/ui && bun test src/components/OTPInput/__tests__/OTPInput.test.tsx` exit 0.

</acceptance_criteria>

---

## Task 3 — `DatePicker`: `locale` + timezone props

<action>

1. Mở rộng `DatePickerProps` (`DatePicker.tsx`) với các field **optional** tương thích upstream (ít nhất):
   - `locale?: string`
   - `timeZoneOffsetInMinutes?: number`
   - `timeZoneOffsetInSeconds?: number` (nếu có trong type của dependency hiện tại)
   - `timeZoneName?: string` (tùy chọn; iOS/Android)
2. Truyền các prop này vào `<DateTimePicker ... />` cùng `value`, `mode`, `onChange`, `minimumDate`, `maximumDate`.
3. JSDoc một dòng: _forward tới `@react-native-community/datetimepicker` — hành vi theo platform._

</action>

<read_first>

- `packages/ui/src/components/DatePicker/DatePicker.tsx`
- `node_modules/@react-native-community/datetimepicker/src/index.d.ts` (xem union `IOSNativeProps` / `AndroidNativeProps`)

</read_first>

<acceptance_criteria>

- `rg "locale\\??" packages/ui/src/components/DatePicker/DatePicker.tsx` — prop trong interface và spread/named trên `DateTimePicker`.
- `cd packages/ui && bun tsc --noEmit` exit 0.

</acceptance_criteria>

---

## Task 4 — `DatePicker` test: forward props

<action>

Mock `DateTimePicker` bằng `jest.mock("@react-native-community/datetimepicker", ...)` trong `DatePicker.test.tsx` **hoặc** assert render (snapshot nhẹ / `toHaveBeenCalledWith`-style) nếu mock sẵn có trong repo.

Tối thiểu: render `<DatePicker locale="vi-VN" timeZoneOffsetInMinutes={420} ... />` và kiểm tra mock picker nhận `locale` / `timeZoneOffsetInMinutes`.

</action>

<read_first>

- `packages/ui/src/components/DatePicker/__tests__/DatePicker.test.tsx`
- Jest preset trong `packages/ui`

</read_first>

<acceptance_criteria>

- `cd packages/ui && bun test src/components/DatePicker/__tests__/DatePicker.test.tsx` exit 0.

</acceptance_criteria>

---

## Verification (post-execution)

- [ ] BL-01: OTP paste — test hook + component
- [ ] BL-02: DatePicker — props + test
- [ ] `05-VALIDATION.md` checklist

---

## PLANNING COMPLETE

**Plans:** 1 (`05-01-PLAN.md`)  
**Waves:** 1

**Deferred (wave 2 / v2):** `NumericScore`, `ReviewSummary`, `SentimentPicker`, `RatingInput` FormField, skeleton shimmer provider — không nằm trong plan này.
