---
wave: 1
depends_on: []
requirements:
  - DS-01
requirements_addressed:
  - DS-01
files_modified:
  - packages/tokens/src/semantic.ts
  - packages/tokens/src/component.d.ts
  - packages/ui/src/components/Typography/Typography.tsx
  - packages/themes/src/brands/love.ts
  - packages/ui/src/components/Switch/Switch.tsx
autonomous: true
gap_closure: false
---

# Plan 04-01: Phase 4 — DS-01 subset (font, surface.hover, loveBrand, Switch tokens)

**Mode:** standard (`04-RESEARCH.md`). **UI hint: yes** — có thể chạy `/gsd-ui-phase 4` trước execute để có UI-SPEC; nếu bỏ qua, executor vẫn làm theo task dưới đây.

**Goal:** Mỗi hạng mục dưới có acceptance grep/tsc/test (đạt success criteria ROADMAP Phase 4).

**must_haves:**

1. `SemanticTokens` có `fontFamily` (sans/mono) và Typography áp dụng `sans` khi có.
2. `darkTokens.color.surface.hover` không còn literal `#1A1A28`; dùng primitive `color.gray[...]`.
3. `loveBrand` light `text.disabled` đạt contrast tối thiểu UI (~3:1) trên nền trắng — giá trị mới + comment.
4. `Switch` dùng `tokens.shadow.sm` (hoặc tương đương) cho thumb + `spacing` cho gap/margin.

---

## Task 1 — `fontFamily` trên semantic `shared` + type `SemanticTokens`

<action>
Trong `packages/tokens/src/semantic.ts`:

1. Thêm vào object **`shared`** (cùng cấp `spacing`, `text`, …):

```typescript
fontFamily: {
  /** Omit / undefined = system default sans */
  sans: undefined as string | undefined,
  mono: undefined as string | undefined,
},
```

2. Mở rộng **`export interface SemanticTokens`** với `fontFamily: typeof shared.fontFamily` (hoặc khai báo struct tương đương).

3. Đảm bảo `lightTokens` / `darkTokens` vẫn spread `shared` — không duplicate literal.

4. Nếu `packages/tokens/src/component.d.ts` hoặc file typedef mirror `SemanticTokens`, cập nhật cho khớp (chỉ các file **đã** mirror semantic trong repo).
</action>

<read_first>
- `packages/tokens/src/semantic.ts` (shared + interface)
- `packages/tokens/src/component.d.ts` (nếu có block semantic)
</read_first>

<acceptance_criteria>
- `rg "fontFamily" packages/tokens/src/semantic.ts` có match trong `shared`.
- `cd packages/tokens && bun tsc --noEmit` exit 0.
</acceptance_criteria>

---

## Task 2 — Typography: áp `fontFamily.sans`

<action>
Trong `packages/ui/src/components/Typography/Typography.tsx`, sau khi resolve `variantStyle`, thêm vào mảng `style` của `<Text>`:

- `tokens.fontFamily.sans != null && tokens.fontFamily.sans !== ""` → `{ fontFamily: tokens.fontFamily.sans }`.

Dùng `useTokens()` — field `fontFamily` đã có sau Task 1.
</action>

<read_first>
- `packages/ui/src/components/Typography/Typography.tsx`
</read_first>

<acceptance_criteria>
- `rg "fontFamily" packages/ui/src/components/Typography/Typography.tsx` có match.
- `cd packages/ui && bun tsc --noEmit` exit 0.
</acceptance_criteria>

---

## Task 3 — Dark `surface.hover` traceable

<action>
Trong `packages/tokens/src/semantic.ts`, **`darkTokens.color.surface.hover`**: thay `"#1A1A28"` bằng **`color.gray[700]`** (hoặc `[750]` nếu đã thêm primitive — ưu tiên **chỉ dùng bậc đã có**) + comment một dòng: *hover một bậc sáng hơn `raised` (gray[800])*.

Sau sửa: `rg "#1A1A28" packages/tokens/src/semantic.ts` không còn match (trừ comment lịch sử — tốt nhất không giữ hex cũ trong comment).
</action>

<read_first>
- `packages/tokens/src/semantic.ts` (darkTokens.surface)
- `packages/tokens/src/primitive.ts` (`color.gray`)
</read_first>

<acceptance_criteria>
- `rg "#1A1A28" packages/tokens/src/semantic.ts` không có match.
- `rg "surface:\\s*\\{[^}]*hover:\\s*color\\.gray" packages/tokens/src/semantic.ts` hoặc đọc file xác nhận `hover: color.gray[` — và `bun tsc --noEmit` trong `packages/tokens` pass.
</acceptance_criteria>

---

## Task 4 — loveBrand `text.disabled` contrast

<action>
Trong `packages/themes/src/brands/love.ts`, **`light.text.disabled`**: đổi từ `#FDA4AF` sang **`#E11D48`** (rose-600) hoặc **`#F43F5E`** (brand rose) — chọn **một** giá trị có contrast ≥ **3:1** trên `#FFFFFF`; thêm comment `// WCAG UI text / disabled`.

Chạy `cd packages/themes && bun tsc --noEmit`.
</action>

<read_first>
- `packages/themes/src/brands/love.ts`
- `AUDIT_REPORT.md` (loveBrand disabled — tham chiếu)
</read_first>

<acceptance_criteria>
- `rg "disabled:" packages/themes/src/brands/love.ts` trong block `text` không còn `#FDA4AF`.
- `cd packages/themes && bun tsc --noEmit` exit 0.
</acceptance_criteria>

---

## Task 5 — Switch: shadow + spacing từ token

<action>
Trong `packages/ui/src/components/Switch/Switch.tsx`:

1. `import` / dùng `useTokens()` (đã có).

2. Thay block shadow cứng trên thumb bằng spread **`...tokens.shadow.sm`** (hoặc chỉ `shadowColor`/`shadowOpacity`/… lấy từ `tokens.shadow.sm` nếu spread gây conflict với `elevation`).

3. `gap: 12` → `gap: tokens.spacing[3]`; `marginTop: 2` → `marginTop: tokens.spacing[0.5]` (primitive có `0.5: 2`).
</action>

<read_first>
- `packages/ui/src/components/Switch/Switch.tsx`
- `packages/tokens/src/semantic.ts` (`shadow.sm`)
</read_first>

<acceptance_criteria>
- `rg 'shadowColor: "#000"' packages/ui/src/components/Switch/Switch.tsx` không có match.
- `rg "spacing\\[3\\]" packages/ui/src/components/Switch/Switch.tsx` có match trên `Pressable` hoặc style row.
- `cd packages/ui && bun tsc --noEmit` exit 0.
</acceptance_criteria>

---

## Task 6 — Smoke tests

<action>
Chạy:

- `cd packages/ui && bun test src/components/Typography/__tests__/Typography.test.tsx src/components/Switch/__tests__/Switch.test.tsx`

Nếu Typography test cần cập nhật (snapshot), cập nhật tối thiểu.
</action>

<read_first>
- `packages/ui/src/components/Typography/__tests__/Typography.test.tsx`
</read_first>

<acceptance_criteria>
- Lệnh test trên exit code 0.
</acceptance_criteria>

---

## Verification (post-execution)

- [ ] DS-01: font + surface.hover + brand disabled + Switch — mỗi mục có acceptance ở trên
- [ ] ROADMAP success criterion (acceptance + test hoặc doc) — tests + grep đủ proof

---

## PLANNING COMPLETE

**Plans:** 1 (`04-01-PLAN.md`)  
**Waves:** 1
