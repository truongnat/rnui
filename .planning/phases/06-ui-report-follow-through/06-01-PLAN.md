---
wave: 1
depends_on: []
phase: 06-ui-report-follow-through
plan: 01
type: execute
requirements:
  - UIREP-01
  - UIREP-02
  - UIREP-03
  - UIREP-04
  - UIREP-05
  - UIREP-06
requirements_addressed:
  - UIREP-01
  - UIREP-02
  - UIREP-03
  - UIREP-04
  - UIREP-05
  - UIREP-06
files_modified:
  - packages/headless/src/theme.tsx
  - packages/headless/src/index.ts
  - packages/headless/src/hooks/usePersistedColorScheme.ts
  - packages/headless/src/hooks/__tests__/usePersistedColorScheme.test.tsx
  - packages/tokens/src/component.ts
  - apps/example/package.json
  - apps/example/app/_layout.tsx
  - apps/example/app/index.tsx
  - packages/ui/src/components/AnimatedList/AnimatedList.tsx
autonomous: true
gap_closure: false
must_haves:
  truths:
    - 'colorScheme survives app restart when using example app'
    - 'Headless exposes a documented persistence hook with tests'
    - 'No new TypeScript warnings for fontWeight in touched example lines'
    - 'FlashList estimatedItemSize guidance is documented on AnimatedList'
  artifacts:
    - path: packages/headless/src/hooks/usePersistedColorScheme.ts
      provides: Storage-agnostic persisted scheme state
    - path: apps/example/app/_layout.tsx
      provides: Theme hydration + persist wiring
---

# Plan 06-01: Phase 6 — Wave 1 (persistence, fontWeight, memo/FlashList audit, haptics)

**Mode:** `06-CONTEXT.md` + `06-RESEARCH.md`. **Nguồn:** `UI_REPORT.md` §6 (High + một Medium nhỏ), §7 (risks).

**Goal:** Ship wave 1: **persist theme**, **headless hook**, **fontWeight TS**, **memo + FlashList risk mitigation**, **haptics trên example** — defer tabs/Storybook đầy đủ / visual regression sang wave 2.

**Owner hints:** `headless` (hook + tests), `tokens` (audit chỉ đọc/sửa tối thiểu), `ui` (JSDoc AnimatedList), `apps/example` (AsyncStorage, haptics, fontWeight).

---

## Deferred (wave 2 / backlog — không nằm trong plan này)

- Example: tab navigation (§6 Medium — replace single scroll với tabs).
- Storybook: story cho từng component (effort Large).
- RTL, custom fonts pipeline, `prefers-reduced-motion` animation tokens (§6 Low).
- **UIREP-07:** Visual regression / snapshot automation — stretch; công cụ (Maestro, Detox screenshot, v.v.) chọn khi mở wave 2.

---

## Task 1 — `usePersistedColorScheme` (headless) + tests

<action>

1. Thêm module `packages/headless/src/hooks/usePersistedColorScheme.ts`:
   - API dạng adapter: `storage: { getItem(key): Promise<string | null>; setItem(key, value): Promise<void> }`, `storageKey`, optional `defaultScheme: ColorScheme | "system"`.
   - Trả về `{ colorScheme, setColorScheme, hydrated }` (hoặc tương đương) tương thích với `ThemeProvider` (`colorScheme` prop + callback khi user đổi scheme).
   - **Không** import AsyncStorage trong headless — chỉ interface; example cung cấp implementation.
2. Export hook từ `packages/headless/src/index.ts` (hoặc barrel đang dùng).
3. Tests: `packages/headless/src/hooks/__tests__/usePersistedColorScheme.test.tsx` — mock storage in-memory; assert hydrate từ saved value, persist khi `setColorScheme`.

**Tránh:** Hard-code `@react-native-async-storage/async-storage` trong `headless` (peer churn).

</action>

<read_first>

- `packages/headless/src/theme.tsx` (`ThemeProviderProps`, `ColorScheme`)
- Pattern test: `packages/headless/src/hooks/__tests__/useOTPInput.test.tsx`

</read_first>

<acceptance_criteria>

- `cd packages/headless && bun test src/hooks/__tests__/usePersistedColorScheme.test.tsx` exit 0.
- `cd packages/headless && bun run typecheck` (hoặc `bun tsc --noEmit` theo script package) exit 0.

</acceptance_criteria>

---

## Task 2 — Example: AsyncStorage + wire `ThemeProvider` (UIREP-01, UIREP-02)

<action>

1. Thêm dependency `@react-native-async-storage/async-storage` vào `apps/example/package.json` (phiên bản tương thích Expo 55 — dùng range docs Expo nếu cần).
2. Trong `apps/example/app/_layout.tsx` (hoặc root layout nơi bọc `ThemeProvider`):
   - Dùng hook Task 1 với `AsyncStorage` làm storage.
   - Chỉ render tree chính sau `hydrated === true` **hoặc** dùng scheme mặc định an toàn để tránh flash — document choice trong code ngắn.
3. Đảm bảo `setColorScheme` từ `useTheme` trong screen vẫn persist (một nguồn sự thật: state từ hook lift vào provider).

**Tránh:** Hai nguồn state cho theme (local `useState` trong index **và** provider) — gộp theo một flow.

</action>

<read_first>

- `apps/example/app/_layout.tsx`
- `apps/example/app/index.tsx` (cách gọi `useTheme`)

</read_first>

<acceptance_criteria>

- `cd apps/example && bun install` (hoặc `bun` tại root workspaces) thành công.
- `cd apps/example && bun run typecheck` exit 0.
- Thủ công ngắn (hoặc ghi trong SUMMARY): kill app → mở lại → scheme giữ như lần trước.

</acceptance_criteria>

---

## Task 3 — `fontWeight` TypeScript consistency (example) — UIREP-03

<action>

1. Trong `apps/example/app/index.tsx`, chuẩn hóa `fontWeight` theo **một** quy ước (khuyến nghị: literal string `"600"` | `"700"` … **hoặc** `700 as TextStyle['fontWeight']` nếu cần số).
2. Gom style lặp thành helper local **chỉ khi** giảm noise (không refactor rộng).
3. Chạy typecheck example.

**Vì sao:** `UI_REPORT.md` §6 — tránh cảnh báo build giữa number/string.

</action>

<read_first>

- `apps/example/app/index.tsx` (các dòng `fontWeight` đã grep)

</read_first>

<acceptance_criteria>

- `cd apps/example && bun run typecheck` exit 0.
- `rg "fontWeight" apps/example/app/index.tsx` — không còn mix ngẫu nhiên không giải thích (document trong comment 1 dòng nếu platform cần exception).

</acceptance_criteria>

---

## Task 4 — Token memo audit (ThemeProvider + `resolveComponentTokens`) — UIREP-04

<action>

1. Đọc `packages/headless/src/theme.tsx`: xác nhận `useMemo` dependencies cho object `theme` (đặc biệt `override` reference).
2. Đọc `packages/tokens/src/component.ts`: ghi nhận `resolveComponentTokens` có pure không, có nên memo ở boundary provider không.
3. **Tối thiểu:** nếu không đổi code — thêm comment JSDoc ngắn trên `ThemeProvider` hoặc `resolveComponentTokens` giải thích chi phí và khi nào `override` nên `useMemo` ở consumer.
4. **Nếu** phát hiện recompute thừa rõ ràng: sửa nhỏ (ví dụ stable deps) trong phạm vi file đã liệt kê.

**Tránh:** Refactor lớn toàn bộ token pipeline trong wave 1.

</action>

<read_first>

- `packages/headless/src/theme.tsx`
- `packages/tokens/src/component.ts` (phần `resolveComponentTokens`)

</read_first>

<acceptance_criteria>

- `cd packages/headless && bun run typecheck` exit 0.
- `cd packages/tokens && bun run typecheck` (nếu có) hoặc `turbo typecheck --filter=@truongdq01/tokens` exit 0.
- Ghi kết luận audit trong `06-01-SUMMARY.md` sau execute (hoặc comment trong code).

</acceptance_criteria>

---

## Task 5 — `AnimatedList`: `estimatedItemSize` documentation + sanity check — UIREP-05

<action>

1. Trong `packages/ui/src/components/AnimatedList/AnimatedList.tsx`, mở rộng JSDoc cho prop `estimatedItemSize`: nêu rõ đây là gợi ý chiều cao item cho FlashList, sai lệch lớn → scroll/perf kém; tham chiếu doc FlashList nếu có URL ổn định.
2. Đối chiếu `apps/example/app/index.tsx` và `apps/storybook/stories/AnimatedList.stories.tsx`: giá trị `estimatedItemSize` có cùng bậc với layout item không (± vài px); chỉnh nếu lệch rõ (ví dụ row ~56 vs 60).

</action>

<read_first>

- `packages/ui/src/components/AnimatedList/AnimatedList.tsx`
- `apps/example/app/index.tsx` (khối `AnimatedList`)
- `apps/storybook/stories/AnimatedList.stories.tsx`

</read_first>

<acceptance_criteria>

- `cd packages/ui && bun run typecheck` exit 0.
- Comment/JSDoc chứa cụm `estimatedItemSize` hoặc `FlashList` (để reviewer tìm).

</acceptance_criteria>

---

## Task 6 — Haptics on example (expo-haptics) — UIREP-06

<action>

1. Trong `apps/example/app/index.tsx` (nút đổi theme Sun/Moon/Monitor hoặc một nhóm tương đương), gọi `Haptics.selectionAsync()` hoặc `impactAsync(Light)` sau khi đổi scheme thành công.
2. Bọc `Platform.OS === "ios"` nếu cần tránh noise Android, **hoặc** dùng `expo-haptics` cross-platform theo doc.
3. Optional: `process.env.EXPO_PUBLIC_RNUI_HAPTICS === "0"` để tắt trong CI — chỉ nếu đơn giản (không bắt buộc wave 1).

**Vì sao:** `UI_REPORT.md` §6 Medium — một lát nhỏ; example đã có `expo-haptics` trong `package.json`.

</action>

<read_first>

- `apps/example/package.json` (expo-haptics)
- `apps/example/app/index.tsx` (theme buttons)

</read_first>

<acceptance_criteria>

- `cd apps/example && bun run typecheck` exit 0.
- Import `expo-haptics` chỉ trong example (không kéo vào `packages/ui` wave 1).

</acceptance_criteria>

---

## Verification (post-execution)

- [ ] UIREP-01..06 theo `06-VALIDATION.md`
- [ ] `bun turbo test` (hoặc filter `headless` + `ui` nếu CI nặng) — ít nhất tests mới + regression gần
- [ ] `bun turbo typecheck` exit 0

---

## PLANNING NOTE

**Plans:** 1 (`06-01-PLAN.md`)  
**Waves:** 1

**Deferred:** Tab nav, Storybook full coverage, RTL/fonts, visual regression (UIREP-07), `accent` button variant trivial (có thể wave 2).
