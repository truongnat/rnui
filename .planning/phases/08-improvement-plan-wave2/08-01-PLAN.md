---
wave: 1
depends_on: []
phase: 08-improvement-plan-wave2
plan: 01
type: execute
requirements:
  - IMP-05
  - IMP-06
  - IMP-07
  - IMP-08
requirements_addressed:
  - IMP-05
  - IMP-06
  - IMP-07
  - IMP-08
files_modified:
  - packages/ui/src/components/Input/Input.tsx
  - packages/ui/src/components/FormField/FormField.tsx
  - packages/ui/src/components/TextArea/TextArea.tsx
  - packages/tokens/src/semantic.ts
  - packages/ui/src/components/Typography/Typography.tsx
  - packages/ui/src/components/Radio/index.ts
  - packages/ui/src/index.ts
  - apps/example/app/index.tsx
autonomous: true
gap_closure: false
must_haves:
  truths:
    - "Input focus border animation <= 120ms (or documented equivalent)"
    - "FormField label/helper spacing and hierarchy per IMPROVEMENT_PLAN"
    - "TextArea counter does not duplicate helperText awkwardly"
    - "Typography supports overline (+ code or documented deferral); example section headers use Typography"
    - "RadioItem exported; Divider vertical + Radio horizontal + Checkbox indeterminate showcased"
---

# Plan 08-01: Phase 8 — Wave 1 (IMPROVEMENT_PLAN Phase 2)

**Nguồn:** `IMPROVEMENT_PLAN.md` *Execution Order → Phase 2 — Component Enhancements*.

**Goal:** **IMP-05..IMP-08** — forms + typography + showcases; **không** Select FlashList / Slider range (defer).

---

## Task 1 — IMP-05: Input + FormField

<action>

1. `Input.tsx`: giảm thời gian animation border focus (mục tiêu ~120ms thay vì 200ms); giữ padding dọc nhất quán (spacing/token).
2. `FormField.tsx`: label `fontSize.sm`, `fontWeight` semibold, màu primary; helper `fontSize.xs`, tertiary; gap label→control `spacing[1.5]`; gap control→helper `spacing[1]`; asterisk required dùng màu lỗi token.

</action>

<acceptance_criteria>

- Không regression test Input/FormField hiện có; `typecheck` ui pass.

</acceptance_criteria>

---

## Task 2 — IMP-06: TextArea counter

<action>

1. Khi có `maxLength`, đặt counter **trong** ô (góc dưới phải) hoặc prop `showCounter` / `counterPosition` theo plan.
2. Tránh trùng thông tin với `FormField` helper — document trong story/example (ví dụ bỏ helper trùng hoặc `showCounter={false}`).

</action>

<acceptance_criteria>

- Example Form + Bio: không còn chồng helper + counter gây nhiễu; typecheck pass.

</acceptance_criteria>

---

## Task 3 — IMP-07: Typography + tokens

<action>

1. `semantic.ts` (hoặc file typography đã dùng): thêm composite styles (display, h1–h6, body1/2, caption, overline, label, code) theo bảng trong `IMPROVEMENT_PLAN.md` Issue #2 (điều chỉnh nhẹ nếu token hiện có đã gần).
2. `Typography.tsx`: variant mới (`overline`, `code`, …); `React.memo` cho style nếu chưa có.
3. `apps/example`: thay `SectionHeader` / nhãn section dùng `<Typography variant="overline">` (hoặc tương đương) thay raw `Text` inline.

</action>

<acceptance_criteria>

- Tests Typography cập nhật nếu snapshot/variant; example đọc rõ hierarchy.

</acceptance_criteria>

---

## Task 4 — IMP-08: Divider + Radio + Checkbox

<action>

1. Export `RadioItem` từ `packages/ui/src/components/Radio/index.ts` và barrel `packages/ui/src/index.ts`.
2. Example: Divider — thêm hàng demo `orientation="vertical"`; Radio — `direction="horizontal"` + optional standalone `RadioItem`; Checkbox — state `indeterminate` như plan Issue #7.

</action>

<acceptance_criteria>

- Export có trong public API; example hiển thị đủ 3 hạng mục.

</acceptance_criteria>

---

## Verification

- `08-VALIDATION.md`
- `bun turbo test --filter=@truongdq01/ui`
- `bun turbo typecheck` cho packages đụng

---

## PLANNING NOTE

**Plans:** 1 (`08-01-PLAN.md`)  
**Next wave (Phase 9+):** Select FlashList, Skeleton presets, EmptyState, Slider, DatePicker — theo `IMPROVEMENT_PLAN.md` Phase 3–4.
