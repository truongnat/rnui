---
wave: 1
depends_on: []
requirements:
  - A11Y-02
  - RATE-03
requirements_addressed:
  - A11Y-02
  - RATE-03
files_modified:
  - packages/ui/src/components/Rating/Rating.tsx
  - packages/ui/src/components/Rating/__tests__/Rating.test.tsx
  - packages/tokens/src/component.ts
  - packages/tokens/src/component.d.ts
autonomous: true
gap_closure: false
---

# Plan 02-01: Phase 2 — Rating depth (a11y, motion, memo, variants)

**Mode:** standard (research in `02-RESEARCH.md`; UI-SPEC skipped — decisions in `2-CONTEXT.md`)

**Goal:** Đạt success criteria ROADMAP Phase 2: `accessibilityRole` / `accessibilityValue`, `React.memo(Rating)`, animation tôn trọng reduce motion, variant read-only/compact/icon theo `RATE-03`.

**must_haves (goal-backward):**

1. Interactive `Rating` có một nút ảo SR (`adjustable` hoặc tương đương đã ghi trong code) với `accessibilityValue` min/max/now; các star không lặp vai trò (`accessible={false}` trên từng star).
2. Đổi giá trị bởi người dùng (tap hoặc `onAccessibilityAction`) gọi `AccessibilityInfo.announceForAccessibility` ít nhất một lần trong test (mock).
3. `export const Rating = memo(...)` (hoặc tương đương) và `Rating.displayName = "Rating"`.
4. Có animation scale khi chọn sao khi reduce motion **tắt**; khi reduce motion **bật** không chạy spring (assert bằng mock hook hoặc snapshot logic).
5. Có ít nhất hai trong ba: `showValue`/`valuePosition`, `compact`, `iconNames` — và test/grep chứng minh.

---

## Task 1 — Wrapper a11y + `accessible={false}` trên star

<action>
Trong `packages/ui/src/components/Rating/Rating.tsx`:

1. Bọc hàng sao trong **một** `View` gốc (giữ `style={rating.container}` hoặc `rating.compact` khi Task 5 thêm token) với:
   - Khi `!readOnly && !disabled`: `accessible={true}`, `accessibilityRole="adjustable"`, `accessibilityValue={{ min: 0, max, now: ratingValue }}`, `accessibilityHint="Swipe up or down to adjust rating"` (chuỗi cố định tiếng Anh; thêm comment `// TODO i18n` nếu chưa có i18n).
   - Khi `readOnly || disabled`: `accessible={true}`, `accessibilityRole="none"`, `accessibilityLabel={`${ratingValue} out of ${max} stars`}` (hoặc chuỗi tương đương một dòng).

2. Mỗi `Pressable` con bọc `Icon`: thêm **`accessible={false}`** và **`importantForAccessibility="no"`** (Android) nếu cần để không tạo 5 control trùng.

3. Thêm **`onAccessibilityAction`** trên wrapper `View` khi interactive: xử lý `event.nativeEvent.actionName === "increment"` và `"decrement"` (và fallback nếu platform dùng tên khác — kiểm tra typings RN). Mỗi bước thay đổi `ratingValue` bằng **`effectivePrecision`** (biến đã có từ `useRating`) làm bước: ví dụ `next = clamp(ratingValue ± effectivePrecision, 0, max)` rồi `setValue(next)` với cùng logic làm tròn như `handlePress` (reuse helper `applyStep(deltaSign)` nội bộ file để tránh lệch).

4. Giữ `handlePress(i)` cho tap; không xóa `Pressable` trừ khi refactor tối thiểu vẫn pass test cũ.
</action>

<read_first>
- `packages/ui/src/components/Rating/Rating.tsx` (toàn bộ)
- `.planning/phases/02-rating-component-depth/2-CONTEXT.md` (D-20–D-21)
- React Native typings: `AccessibilityActionEvent` / `onAccessibilityAction` trong `node_modules/react-native` (chỉ đọc phần liên quan)
</read_first>

<acceptance_criteria>
- `rg 'accessibilityRole="adjustable"' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- `rg 'accessible=\{false\}' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match (trên `Pressable` star).
- `rg 'onAccessibilityAction' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- `rg 'accessibilityValue' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- `cd packages/ui && bun tsc --noEmit` exit code 0.
</acceptance_criteria>

---

## Task 2 — `announceForAccessibility` khi người dùng đổi giá trị

<action>
Trong cùng file `Rating.tsx`:

1. `import { AccessibilityInfo } from "react-native"`.

2. Tạo helper **`announceValue(next: number)`** gọi `AccessibilityInfo.announceForAccessibility(\`${next} out of ${max} stars\`)` (chuỗi cố định + `// TODO i18n`).

3. Gọi `announceValue` **chỉ** từ:
   - `handlePress` sau khi `setValue` đã xác định giá trị mới (dùng biến `next` sau snap), và
   - handler `onAccessibilityAction` sau khi `setValue` với giá trị mới.

4. **Không** gọi trong `useEffect` theo `value` từ props (tránh announce khi parent đổi controlled value từ code).
</action>

<read_first>
- `packages/ui/src/components/Rating/Rating.tsx` (sau Task 1)
- `2-CONTEXT.md` (D-22)
</read_first>

<acceptance_criteria>
- `rg 'announceForAccessibility' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- `rg 'AccessibilityInfo' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- Không có `useEffect` nào trong file chứa `announceForAccessibility` (verify: `rg -n 'useEffect' packages/ui/src/components/Rating/Rating.tsx` và `rg 'announceForAccessibility' packages/ui/src/components/Rating/Rating.tsx` — nếu có `useEffect`, nó không được gọi `announceForAccessibility`).
</acceptance_criteria>

---

## Task 3 — `React.memo` + `displayName`

<action>
1. Đổi `export function Rating` thành `function RatingInner` (tên nội bộ tùy chọn), sau đó `export const Rating = memo(RatingInner)`; đặt `Rating.displayName = "Rating"`.

2. Export type: giữ `export interface RatingProps` và dùng `export type { RatingProps }` nếu cần — barrel `index` không gãy.

3. Thêm ngắn trong JSDoc trên `RatingProps`: consumer nên **ổn định `onChange`** với `useCallback` khi truyền vào để tránh re-render không cần (memo shallow).
</action>

<read_first>
- `packages/ui/src/components/Rating/Rating.tsx`
- `packages/ui/src/index.ts` (chỉ kiểm tra export `Rating` vẫn tồn tại)
</read_first>

<acceptance_criteria>
- `rg 'memo\(' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- `rg 'displayName = "Rating"' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- `rg "export.*Rating" packages/ui/src/index.ts` vẫn re-export `Rating` (hoặc path barrel tương đương dự án đang dùng).
</acceptance_criteria>

---

## Task 4 — Scale feedback + reduce motion

<action>
1. `import { useReduceMotionEnabled } from "@truongdq01/headless"`.

2. Trên mỗi star, bọc nội dung `Icon` trong **`Animated.View`** từ `react-native-reanimated` (hoặc `Reanimated` `Pressable` nếu đã có pattern trong repo). Dùng `useSharedValue(1)` cho scale của star đang nhấn (index khớp `pressedIndex` state hoặc shared value).

3. `onPressIn` / `onPressOut` trên `Pressable`: khi `!reduceMotion`, `scale` tới `1.12` (hoặc giá trị cụ thể) bằng `withSpring` với `spring` từ `@truongdq01/tokens` hoặc `spring.bouncy` đã dùng ở component khác — **chỉ định trong code** tên import (ví dụ `import { spring } from "@truongdq01/tokens"` hoặc đường dẫn đúng trong repo).

4. Khi `reduceMotion === true`: **không** gọi `withSpring`; giữ scale `1`.

5. Đảm bảo `readOnly || disabled` không chạy animation scale.
</action>

<read_first>
- `packages/ui/src/components/Rating/Rating.tsx`
- `packages/tokens/src/motion.ts` hoặc `packages/tokens/src/index.ts` (export `spring`)
- `packages/headless/src/hooks/useMotionPreference.ts`
</read_first>

<acceptance_criteria>
- `rg 'useReduceMotionEnabled' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- `rg 'withSpring|withTiming' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match (một trong hai).
- `rg 'react-native-reanimated' packages/ui/src/components/Rating/Rating.tsx` có ít nhất 1 match.
- `cd packages/ui && bun tsc --noEmit` exit code 0.
</acceptance_criteria>

---

## Task 5 — `showValue`, `valuePosition`, `compact`, `iconNames`

<action>
1. Mở rộng **`RatingProps`**:
   - `showValue?: boolean` (default `false`)
   - `valuePosition?: "start" | "end"` (default `"end"`)
   - `compact?: boolean` (default `false`)
   - `iconNames?: { filled: IconName; empty: IconName; half: IconName }` với default **`{ filled: "star", empty: "star", half: "starHalf" }`** — comment JSDoc: empty dùng cùng glyph `star` với màu `rating.star.empty.color`.

2. **`showValue`:** khi `true`, render `Text` hoặc `Typography` (theo pattern component lân cận) hiển thị **`${ratingValue}/${max}`** cạnh hàng sao; vị trí `start` | `end` theo prop.

3. **`compact`:** khi `true`, giảm gap — ưu tiên thêm **`ratingTokens.container.compactGap`** (hoặc `gap` số cụ thể) trong `packages/tokens/src/component.ts` + `component.d.ts` và map trong `Rating` (`style={[rating.container, compact && rating.containerCompact]}` hoặc tương đương). Nếu token mới: cập nhật `ComponentTokens` nếu repo bắt buộc.

4. **`iconNames`:** thay thế literal `"star"` / `"starHalf"` trong logic chọn icon bằng `iconNames.filled`, `iconNames.half`, `iconNames.empty` với fallback default ở destructuring props.
</action>

<read_first>
- `packages/tokens/src/component.ts` (`ratingTokens` hiện tại)
- `packages/tokens/src/component.d.ts`
- `packages/ui/src/components/Rating/Rating.tsx`
- `packages/ui/src/components/Typography/Typography.tsx` (chỉ nếu chọn dùng Typography — một dòng import)
</read_first>

<acceptance_criteria>
- `rg 'showValue' packages/ui/src/components/Rating/Rating.tsx` có match.
- `rg 'compact' packages/ui/src/components/Rating/Rating.tsx` có match.
- `rg 'iconNames' packages/ui/src/components/Rating/Rating.tsx` có match.
- `rg 'valuePosition' packages/ui/src/components/Rating/Rating.tsx` có match.
- `rg 'ratingTokens' packages/tokens/src/component.ts` vẫn chứa `rating` và file compile: `cd packages/tokens && bun tsc --noEmit` exit 0 (hoặc `turbo` tương đương dự án).
</acceptance_criteria>

---

## Task 6 — Tests + smoke

<action>
Cập nhật `packages/ui/src/components/Rating/__tests__/Rating.test.tsx`:

1. Mock `AccessibilityInfo.announceForAccessibility` = `jest.fn()` trong `beforeEach`.

2. Test: render interactive `Rating` với `onChange`; fire press; expect mock announce được gọi với string chứa `out of` và `max` (regex hoặc `toContain`).

3. Test: `getByA11yRole` hoặc `UNSAFE_getAllByType` — wrapper có `adjustable` khi không `readOnly`; star `Pressable` có `accessible={false}` (query props qua `toJSON` hoặc `UNSAFE_getByProps` tùy RTL version).

4. Test: `showValue` render text chứa `/` hoặc `max`; `compact` hoặc `iconNames` ít nhất một assertion (snapshot hoặc query).

5. Chạy: `cd packages/ui && npx jest --config jest.config.js src/components/Rating/__tests__/Rating.test.tsx --no-cache`.
</action>

<read_first>
- `packages/ui/src/components/Rating/__tests__/Rating.test.tsx`
- `packages/ui/src/components/Pressable/__tests__/Pressable.test.tsx` (mẫu mock RN)
</read_first>

<acceptance_criteria>
- Lệnh `cd packages/ui && npx jest --config jest.config.js src/components/Rating/__tests__/Rating.test.tsx` exit code 0.
- File test chứa `announceForAccessibility` (grep) hoặc biến mock tên `announceMock`.
- `rg 'showValue|compact|iconNames' packages/ui/src/components/Rating/__tests__/Rating.test.tsx` có ít nhất 2 trong 3 từ khóa.
</acceptance_criteria>

---

## Verification (post-execution)

- [ ] ROADMAP Phase 2 success criterion 1 (role/value) — đạt
- [ ] ROADMAP Phase 2 success criterion 2 (memo + reduce motion) — đạt
- [ ] ROADMAP Phase 2 success criterion 3 (variant) — đạt
- [ ] REQUIREMENTS **A11Y-02**, **RATE-03** — có trong frontmatter và được cover bởi task 1–6

---

## PLANNING COMPLETE

**Plans:** 1 (`02-01-PLAN.md`)  
**Waves:** 1 (tuần tự Task 1 → 6 trong một PR)
