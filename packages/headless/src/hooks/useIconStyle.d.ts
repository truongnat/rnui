/**
 * Hook to apply default icon styles (size, color) based on component context.
 * Returns an object with style and color for the icon.
 */
export declare function useIconStyle(context: "button" | "input" | "list" | "select"): {
    size: 15;
    color: string;
} | {
    size: 17;
    color: string;
};
//# sourceMappingURL=useIconStyle.d.ts.map