/** Extra padding below safe-area inset on the sheet container. */
export const BOTTOM_SHEET_EXTRA_BOTTOM_PADDING = 8;

/** Default snap when the consumer does not pass `snapPoints`. */
export const BOTTOM_SHEET_DEFAULT_SNAP_POINTS = ["50%"] as const;
