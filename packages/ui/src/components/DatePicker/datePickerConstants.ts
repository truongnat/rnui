/** Shared layout & interaction defaults — common mobile date-picker UX. */

/** Primary touch target (iOS HIG / Material ~48dp). */
export const DATE_PICKER_FIELD_HEIGHT = 48;

/** Header / footer row minimum height. */
export const DATE_PICKER_SHEET_ROW_MIN_HEIGHT = 48;

/** Leading / trailing header slots so title stays visually centered. */
export const DATE_PICKER_HEADER_SLOT = 44;

export const DATE_PICKER_SHEET_RADIUS = 16;
export const DATE_PICKER_DRAG_HANDLE_WIDTH = 36;
export const DATE_PICKER_DRAG_HANDLE_HEIGHT = 4;

export const DATE_PICKER_BACKDROP = "rgba(0,0,0,0.4)";

/** Years shown in year grid (centered on current year). */
export const DATE_PICKER_YEAR_SPAN = 12;
